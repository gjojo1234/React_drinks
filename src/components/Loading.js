import React from "react";
import Wrapper from "../wrappers/loadingWrapper";

const Loading = () => {
  return (
    <Wrapper>
      <h1>Loading.....</h1>
    </Wrapper>
  );
};

export default Loading;
